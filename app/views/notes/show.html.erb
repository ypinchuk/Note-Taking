<p>
    <strong>Title:</strong>
    <%= @note.title %>
</p>

<p>
    <strong>Text:</strong>
    <%= @note.text %>
</p>

<h2>Comments</h2>
<%= render @note.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Note List', notes_path %>